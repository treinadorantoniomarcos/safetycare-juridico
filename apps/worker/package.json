{
  "name": "@safetycare/worker",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node --import=tsx src/index.ts",
    "build": "tsc -p tsconfig.json",
    "lint": "node -e \"console.log('worker lint placeholder')\"",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "dependencies": {
    "@safetycare/database": "*",
    "@safetycare/observability": "*",
    "@safetycare/orchestrator": "*"
  },
  "devDependencies": {
    "tsx": "^4.19.0",
    "typescript": "^5.7.0",
    "vitest": "^3.2.4"
  }
}
